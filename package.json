{
  "name": "sea-battle",
  "version": "1.0.0",
  "description": "Улучшенный морской бой — браузерная пошаговая игра с линейкой",
  "main": "server/index.js",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env server/index.js",
    "dev": "node --env-file-if-exists=.env --watch server/index.js",
    "db:up": "docker run -d --name sb-mysql -e MYSQL_ROOT_PASSWORD=root -e MYSQL_DATABASE=seabattle -p 3307:3306 -v sb-mysql-data:/var/lib/mysql mysql:8",
    "db:down": "docker rm -f sb-mysql"
  },
  "dependencies": {
    "express": "^4.21.2",
    "google-auth-library": "^10.7.0",
    "mysql2": "^3.22.5",
    "nodemailer": "^8.0.11",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "socket.io-client": "^4.8.3"
  }
}
